# Enable USB host support
CONFIG_USB=y

# Enable USB Serial Converter support
CONFIG_USB_SERIAL=y

# Enable USB driver for generic serial devices
CONFIG_USB_SERIAL_GENERIC=y

# Enable USB driver for GSM and CDMA modems
CONFIG_USB_SERIAL_OPTION=y

# Enable CDC ACM support (for USB modems)
CONFIG_USB_ACM=y
