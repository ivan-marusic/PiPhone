image sdcard.img {
  hdimage {
    partition-table-type = "msdos"
  }

  partition boot {
    image = "boot.vfat"
    partition-type = 0xC
    bootable = "true"
    size = 64M
  }

  partition rootfs {
    image = "rootfs.ext4"
    partition-type = 0x83
    size = 512M
  }
}
